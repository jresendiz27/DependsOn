import spock.lang.Specification


class DependsOnSpec extends Specification {
    def setup() {

    }

    void "Should add a dependency and create the structure for it children"() {
        expect:
        assert false
    }

    void "Should list all dependencies"() {
        expect:
        assert false
    }

    void "Should install a dependency"() {
        expect:
        assert false
    }

    void "Should remove a dependency with no conflicts"() {
        expect:
        assert false
    }

    void "Should remove a dependency with conflicts"() {
        expect:
        assert false
    }
}
